var React = require('react');
// Require modules being loaded here

var App = React.createClass({
  render: function() {
    return (
      <div>
        // Entry point for the rest of the app. Load from here
      </div>
    )
  }
});

module.exports = App;