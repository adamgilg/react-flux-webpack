var Dispatcher = require('../dispatcher/Dispatcher')
  , Constants  = require('../constants/Constants');

// Actions for a specific store - rename to use with store
var BasicActions = {
  // get: function(url) {
  //   Dispatcher.dispatch({
  //     actionType: Constants.GET,
  //     url: url
  //   })
  // }
};

module.exports = BasicActions;